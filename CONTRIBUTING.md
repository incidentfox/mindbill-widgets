# Contributing

1. Read `AGENTS.md` and use synthetic data only.
2. Install with `corepack enable && pnpm install --frozen-lockfile`.
3. Make the smallest compatible change and update public docs.
4. Run `pnpm check` and `pnpm pack:check`.
5. Open a pull request. Do not publish packages from a workstation.
